<!-- BD_DEMO: borrar este bloque si no hay overrides, o reemplazar con overrides reales. -->

# Overrides — {{TITLE}}

Por defecto: **ninguno**. La landing usa `design-system/tokens.css`.

Solo lista variables si esta campaña necesita un ajuste local (ej. accent de temporada). Nunca edites el global.

```css
/* ejemplo — borrar si no aplica */
--accent: #b8e1ec;
```

## Prompt sugerido (otro chat)

```
¿La campaña "{{TITLE}}" necesita tokens distintos al design system Bodecatta (fondo oscuro, accent #b8e1ec, Bebas + Hanken)?
Si no, responde exactamente: "Sin overrides."
Si sí, lista solo variables CSS a sobreescribir en el root de la landing y justifica en una línea cada una.
```
